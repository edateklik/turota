# Administration Module

Identity `Admin` policy ile korunan yönetim use-case'lerinin sınırıdır. Mevcut Discovery CRUD
endpointleri `/api/admin` altında bu policy ile korunmaktadır. İstatistik ve simülasyon
use-case'leri sonraki adımlarda buraya taşınacaktır.
